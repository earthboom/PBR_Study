# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## 세션 시작 시 필독

**새 세션을 시작할 때 반드시 `PROGRESS.md`를 먼저 읽어라.**
현재 어느 챕터를 진행 중인지, 막혔던 부분은 무엇인지 파악한 뒤 이어서 진행한다.
전체 학습 전략은 `LEARNING_STRATEGY.md`를 참고한다.

---

## 프로젝트 개요

UE5 클라이언트 프로그래머가 **그래픽스 프로그래머로 이직**하기 위한 PBR 실습 학습 공간.
실습 결과물은 GitHub 포트폴리오로 제출 예정.

### 디렉토리 구조

```
PBR_Study/
├── CLAUDE.md
├── LEARNING_STRATEGY.md      ← 전체 4 Phase 학습 계획
├── PROGRESS.md               ← 현재 진행 상황 (세션 시작 시 필독)
├── PHASE1_NOTES.md           ← Phase 1 챕터별 학습 노트
├── generate_diagrams.py      ← 학습 다이어그램 생성 스크립트
├── diagrams/                 ← 생성된 다이어그램 이미지
├── pbr4ed/                   ← PBR 4th Edition 웹버전 로컬 저장본
└── pbr-raytracer/            ← Phase 1 실습 프로젝트
    ├── CMakeLists.txt
    ├── results/              ← 챕터별 결과 이미지 (GitHub에 커밋됨)
    ├── output/               ← 렌더링 출력 폴더 (.gitignore 제외됨)
    └── src/
        ├── main.cpp
        ├── rtweekend.h
        ├── vec3.h
        ├── ray.h
        ├── color.h
        ├── interval.h
        ├── hittable.h
        ├── hittable_list.h
        ├── sphere.h
        └── camera.h
```

### 빌드 방법

Rider에서 CMake 프로파일(Debug-Visual Studio)로 빌드 및 실행.
출력 이미지는 `pbr-raytracer/output/` 폴더에 저장됨 (코드에서 자동 생성).

PPM → PNG 변환 (`py` 명령 사용, `python`은 동작하지 않음):
```
py -c "from PIL import Image; Image.open('pbr-raytracer/output/chN_xxx.ppm').save('pbr-raytracer/output/chN_xxx.png')"
```

---

## 협업 규칙

### 코드 작성 역할 분담

- **핵심 알고리즘 코드** (교차 계산, 셰이딩, 수학 유틸리티 등)는 사용자가 직접 타이핑한다.
  - 참고 코드를 보면서 타이핑하되, 각 줄을 치기 전에 "이 줄의 역할이 무엇인가"를 한 번 생각한다.
  - 복사-붙여넣기는 하지 않는다.
- **보일러플레이트 코드** (파일 입출력, CMake 설정, 진행 표시 등)는 Claude가 작성한다.

### 코드 주석 기준

- 주석은 한국어로 작성한다 (CMakeLists.txt에 `/utf-8` 옵션 적용되어 있음).
- IntelliSense 경고(`#include errors`)는 실제 빌드 오류가 아니므로 무시한다.
- "무엇을 하는가"만 쓰지 않는다. 수학적 의미가 있는 코드에는 "왜 이렇게 되는가"를 덧붙인다.

| 나쁜 주석 | 좋은 주석 |
|-----------|-----------|
| `// oc를 계산한다` | `// oc = 카메라→구중심 벡터. 광선 출발점 기준으로 구가 어느 방향에 있는지` |
| `// 판별식이 음수면 return` | `// 판별식 < 0 → 허수근 → 광선이 구를 비껴감` |

수학 공식이 코드에 직접 나타나는 경우, 기호와 변수를 1:1로 연결하는 주석을 단다:
```cpp
// t = (h ± √(h²-ac)) / a  에서 가까운 근(- 부호)부터 시도
double root = (h - sqrtd) / a;
```

### 챕터 완료 시 필수 체크리스트

챕터 구현이 끝나고 빌드/실행이 확인되면, **반드시 아래 순서대로** 처리한다. 하나라도 빠뜨리지 않는다.

**1. 다이어그램 후보 목록 확정**
- 노트 작성을 시작하기 전에, 이번 챕터에서 새로 등장한 **기하학적/물리적 개념 목록**을 먼저 채팅에 나열한다.
- 예: "이번 챕터의 다이어그램 후보: ① 반사 벡터, ② fuzz 효과"
- 목록을 확정한 뒤에만 다음 단계로 넘어간다.

**2. `generate_diagrams.py` 업데이트 및 실행**
- 1번에서 확정한 각 개념에 대한 다이어그램 코드를 추가한다.
- 추가 즉시 `py generate_diagrams.py`를 실행한다.
- **`diagrams/` 폴더에 해당 PNG 파일이 실제로 존재하는지 확인한다.**
- PNG 파일이 존재하지 않으면 노트 작성을 시작하지 않는다.

> **위반 사례 (Ch.10)**: 노트와 다이어그램 코드를 동시에 작성했으나 `py generate_diagrams.py` 실행을 누락했다.
> 사용자가 지적한 뒤에야 다이어그램을 생성하고 노트에 링크를 추가했다.
> 원인: "코드를 추가했다"와 "이미지가 생성됐다"를 구분하지 않은 것.
> 방지책: **PNG 파일 존재 여부를 확인한 뒤에만 노트 작성을 시작한다.**

**3. `PHASE1_NOTES.md` 업데이트**
- 이번 챕터에서 배운 모든 수학 개념을 아래 "수학 설명 7단계"에 따라 기록한다.
- 개념 설명의 "머릿속 그림" 또는 "코드와 연결" 직후에 해당 다이어그램을 `![설명](diagrams/파일명.png)` 형식으로 삽입한다.
- 결과 이미지를 `pbr-raytracer/results/chN_xxx.png`로 복사한 뒤 그 경로로 링크한다.
  - `output/`은 `.gitignore` 제외 대상이라 GitHub에 올라가지 않는다. 반드시 `results/`를 써야 한다.
- 관련 파일 목록(`### 관련 파일` 섹션)을 포함한다.

**4. `PROGRESS.md` 업데이트**
- 완료된 챕터 체크박스를 `[x]`로 변경한다.
- 헤더의 "다음 작업"과 `← 현재 위치` 표시를 다음 챕터로 이동한다.

**5. 사용자에게 다음 챕터 진행 여부를 묻는다.**

---

## 수학 설명 원칙

사용자는 UE5 클라이언트 프로그래머 출신으로 수학 배경이 강하지 않다.
**수학 개념을 설명할 때는 아래 7단계를 반드시 순서대로 따른다. 단계를 건너뛰지 않는다.**

### 7단계 설명 순서

1. **왜 이 수학이 필요한가** — "이 수식 없이는 레이트레이서의 어떤 문제가 해결 안 되는가"를 먼저 말한다. 수식보다 동기가 먼저다.

2. **머릿속 그림** — 눈을 감고 상상할 수 있는 물리적 장면으로 설명한다. 수식이 나오기 전에 독자의 머릿속에 그림이 있어야 한다.

3. **일상 언어 비유** — 중학생에게 설명하듯이 쓴다. 수학 용어 없이 핵심을 한 문장으로 전달한다.

4. **수식 전개 (빈 단계 없이)** — "전개하면", "정리하면" 같은 말로 중간 단계를 건너뛰지 않는다. 독자가 "이 줄에서 저 줄로 어떻게 갔지?"라는 의문이 생기지 않아야 한다.

5. **수식이 의미하는 것** — 수식 다음에 반드시 "이 수식은 ~을 뜻한다"를 한국어로 한 문장 이상 설명한다. 수식만 던지고 끝내지 않는다.

6. **구체적 숫자 예시** — 실제 숫자를 대입해 수식이 맞는지 직접 확인한다.

7. **코드와 1:1 연결** — 수식의 각 기호가 코드의 어느 변수/함수인지 표로 명시한다.

### 절대 하지 말아야 할 것

- 수식을 먼저 쓰고 나중에 설명하는 것 — "왜"와 "그림"이 수식보다 반드시 앞에 온다.
- "직관적으로 알 수 있듯이", "간단히 전개하면" 같은 말로 중간 단계를 건너뛰는 것.
- 기호를 정의하지 않고 쓰는 것 — 모든 기호는 처음 등장할 때 한국어로 설명한다.
- 수식 나열만 하고 "이게 무슨 뜻인지"를 쓰지 않는 것.

### 채팅 응답에서의 수식 표기

- **LaTeX (`$$...$$`, `$...$`)를 쓰지 않는다** — VSCode 채팅창에서 렌더링되지 않고 raw 텍스트로 보인다.
- 대신 유니코드 기호와 일반 텍스트를 쓴다: `²`, `√`, `·`, `≤`, `≥`, `→`, `×`
- 예: `P(t) = O + t·d`, `|P - C|² = r²`, `판별식 = h² - a·c`
- `PHASE1_NOTES.md` 같은 마크다운 문서에는 LaTeX를 써도 된다 (GitHub에서 렌더링됨).

---

## PHASE1_NOTES.md 작성 기준

**대상 독자: 수학을 오래 안 써서 감이 없는 프로그래머. 읽으면 혼자 이해할 수 있는 수준으로 쓴다.**

위의 "7단계 설명 순서"를 그대로 적용한다. 추가로:

- **표 활용** — 여러 경우를 비교할 때(판별식 부호, 법선 방향별 색상 등)는 반드시 표로 정리한다.
- **중간 요약** — 긴 수식 전개 중간에 "지금까지 구한 것: t에 대한 이차방정식" 같은 한 줄 요약을 넣는다.
- **다이어그램** — 새로운 기하학적 개념이 등장하면 `generate_diagrams.py`에 다이어그램을 추가하고 이미지 링크를 노트에 포함한다.

### 수식 작성 규칙

- `$$...$$` 블록 안에서 줄 맨 앞에 `+`를 쓰지 않는다 — 마크다운이 리스트로 오인한다. `\begin{aligned}` 환경을 사용한다.
- **`\text{}` 안에 언더스코어(`_`)를 절대 쓰지 않는다** — GitHub KaTeX 렌더러가 에러를 낸다.
  - 나쁜 예: `\text{pixel\_center}`, `\text{focal\_length}`, `\text{viewport\_height}`
  - 좋은 예: 코드 변수명은 백틱으로 쓰거나 수식 기호로 대체한다 (`P_{ij}`, `f` 등).
  - 수식 안에서 변수명을 쓸 때는 `\text{focal length}` (공백) 또는 짧은 기호(`f`, `h`)로 치환하고 본문에서 "여기서 $f$ = `focal_length`" 식으로 정의한다.
- 수식 다음에는 반드시 한국어로 "이 수식이 의미하는 것"을 한 문장 이상 설명한다.

### 좋은 설명의 기준

`PHASE1_NOTES.md`의 **Ch.4 — 광선-구 교차** 섹션이 기준이다.
① 왜 필요한가 → ② 머릿속 그림 → ③ 비유 → ④ 수식 전개(빈 단계 없이) → ⑤ 수식의 의미 → ⑥ 숫자 예시 → ⑦ 코드 연결(표)
이 구조를 모든 수학적 개념 설명의 기본 틀로 삼는다.
